{
  "name": "greenconnect-root",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "client:dev": "npm run dev --prefix client",
    "server:dev": "cross-env PORT=5188 npm run dev --prefix server",
    "dev": "concurrently -n client,server -c green,cyan \"npm:client:dev\" \"npm:server:dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3"
  }
}


